{{! This is the post loop - each post will be output using this markup }}
{{#foreach posts}}
  {{#if featured}}
  {{#if @odd}}
      {{> "single-loop-post"}}
  {{else}}
  {{> "single-loop-post-rev"}}

    {{/if}}

  {{/if}}

{{/foreach}}
{{#foreach posts}}
  {{^if featured}}
  {{#if @odd}}
      {{> "single-loop-post"}}
  {{else}}
  {{> "single-loop-post-rev"}}

    {{/if}}

  {{/if}}

{{/foreach}}
{{> "advertisement"}}
